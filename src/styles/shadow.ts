import {Height, Width} from "../styles/demention.ts";

export const shadow = {
    shadowOffset: {height: Height / 200, width: Width / 200,},
    shadowColor: "#000",
    shadowOpacity: 0.15,
    shadowRadius: Width / 70,
}
